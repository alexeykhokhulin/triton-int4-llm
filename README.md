# triton-int4-llm
Triton int4 kernels for LLMs: fp16→int4 quantization, packed storage, and bf16×int4 GEMM for fast inference.

# Setup guide
```
sudo apt-get update
sudo apt-get install -y python3.10-dev

poetry install
```